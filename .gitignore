# Dependencias
node_modules/

# Variables de entorno (NUNCA subir al repo)
.env
.env.local
.env.production

# WhatsApp auth (sesión local — se regenera con QR en el servidor)
.wwebjs_auth/
.wwebjs_cache/

# Frontend compilado (Railway lo construye solo)
dist/

# Logs y temporales
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# Claude
.claude/
package-lock.json
